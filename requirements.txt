# ── Core framework ───────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# ── ML / Deep learning ──────────────────────────────────────────────
# For CPU-only (smaller download), install with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Otherwise, install normally for GPU support:
#   pip install torch torchvision
torch
torchvision

# ── Image handling ───────────────────────────────────────────────────
Pillow==10.4.0
